const {request,response} = require('express');
const {db} = require('../config/firebase');

const paginaInicio = async (req = request,res = response) =>{

    console.log('olis')
}






module.exports = {
    paginaInicio
}